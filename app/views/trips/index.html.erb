<div class="w-full flex flex-col items-center">
   <h1 class="font-bold text-4xl text-center">Trips</h1>
	 <%= link_to "Buy new", checkout_path, class: "rounded-full py-1 px-2 bg-green-600 text-white font-medium mt-4", data: { turbo: false } %>
   <p class="font-medium text-gray-500 text-center text-xs sm:text-sm mt-1 px-4">
		 Make a trip current if it's the main one you're working on
	 </p>
  

  <div id="trips" class="min-w-full flex flex-wrap gap-4 justify-center mt-8">
    <%= render @trips %>
  </div>
</div>
