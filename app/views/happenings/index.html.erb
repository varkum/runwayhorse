<div class="w-full">
	<div class="relative mb-12">
		<h1 class="font-bold text-4xl text-center">Transportation</h1>
		<%= link_to new_trip_transportation_path, class: "flex items-center justify-center absolute top-0 right-0 mr-4 bg-green-600 text-white px-3 py-2 rounded", "data-admin": "true" do %>
			<%= image_tag "plus.svg", class: "h-5 w-auto mr-1" %>
			<p class="font-light text-sm">New</p>
		<% end %>
	</div>
	
	<% if @happenings.any? %>
		<div class="px-4 sm:px-40">
	  	<%= render @happenings, day_by_day: false %>
		</div>
	<% else %>
		<p class="mx-auto max-w-sm px-4 font-medium text-gray-500">
			Nothing to show yet. Start by adding flights to and from home!
		</p>
	<% end %>
</div>
